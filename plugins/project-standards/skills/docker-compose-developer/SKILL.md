---
name: docker-compose-developer
description: Use when adding or changing Dockerfiles, Compose services, images, containers, networks, volumes, health checks, environment wiring, or local container lifecycle.
---

# Docker Compose Developer

Read `references/docker-compose.md` completely.

Keep image build, runtime configuration, persistent state, secrets, health, dependencies, and cleanup ownership explicit. Use exact artifact identities for destructive lifecycle actions and verify the final Compose model and live behavior.
