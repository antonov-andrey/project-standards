---
name: legacy-python-maintainer
description: Use when fixing, containing, deleting, or assessing Python code classified as Legacy without an explicitly requested migration into the modern project model.
---

# Legacy Python Maintainer

Read `references/legacy-python.md` completely.

Keep behavior working inside the existing owner boundary. Do not turn routine maintenance into a structural migration, create new long-lived legacy owners, or apply modern structure retrofits unless the user explicitly requests that migration.
